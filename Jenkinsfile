pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/<your-MeghanaGangavarapu>/<devops>.git'
            }
        }

        stage('Build') {
            steps {
                echo "Bulid successful"
            }
        }
    }
}
